package org.example.weatherapp.dto.weatherperiodanalyzer.yieldforecast

import org.example.weatherapp.dto.weatherperiodanalyzer.RequiredData

class YieldForecastBody(
    private val requiredData: RequiredData,
) {
}